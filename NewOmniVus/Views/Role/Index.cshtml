@model IEnumerable<IdentityRole>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<h1 class="text-light text-center">Roles</h1>

@foreach (var role in Model)
{
    <p>@role.Name</p>
}